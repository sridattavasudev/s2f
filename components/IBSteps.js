import React from "react";
import { Text, View } from "react-native";

export default function IBSteps() {
  return (
    <View>
      <Text style={{ fontSize: 17, textAlign: "justify" }}>
        Consideration phase (0-30 days) schools conveys expression of interest
        for IBPYP nad provide basic contact information via the indication of
        interest from Request for candidacy(30-60 days) IB decision on
        candidacy(60-90 days)
      </Text>
    </View>
  );
}
